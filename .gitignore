# Soft links to real config files and data in the deployment directory
# (Chatterbot modifies the bot config script with since_id, so it's not static)
WheresThatSat.yml
/config
/tle

# Default bot config (contains API keys)
/default-config/WheresThatSat.yml

# Contains the schedule for running WheresThatSat and UpdateCatalog
/wts_crontab

# Local copy of gtg
/gtg

# Reference copy of Chatterbot source code
/chatterbot

# Bot icon artwork scratch files
/Icon
